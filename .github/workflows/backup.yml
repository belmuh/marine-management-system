name: DB Backup — Neon → R2

# Her gece 02:00 UTC'de otomatik çalışır.
# Manuel tetikleme: Actions sekmesi → "DB Backup" → Run workflow
on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  backup:
    name: pg_dump → Cloudflare R2
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: postgresql-client kur
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y postgresql-client

      - name: AWS CLI kur (R2, S3-uyumlu)
        run: pip install awscli --quiet

      - name: pg_dump al ve sıkıştır
        env:
          # Neon bağlantı stringi: PostgreSQL formatında (JDBC değil)
          # Örnek: postgresql://user:pass@ep-xxx.eu-central-1.aws.neon.tech/dbname?sslmode=require
          # GitHub → Settings → Secrets → Actions → NEON_DATABASE_URL
          DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
        run: |
          FILENAME="backup-$(date -u +%Y%m%d-%H%M%S).sql.gz"
          echo "Backup dosyası: $FILENAME"
          pg_dump "$DATABASE_URL" \
            --no-password \
            --verbose \
            --format=plain \
            --no-owner \
            --no-privileges \
          | gzip > "$FILENAME"
          echo "BACKUP_FILE=$FILENAME" >> $GITHUB_ENV
          ls -lh "$FILENAME"

      - name: R2'ye yükle
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_BACKUP_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_BACKUP_SECRET_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BACKUP_BUCKET }}
        run: |
          aws s3 cp "$BACKUP_FILE" \
            "s3://${R2_BUCKET}/db-backups/${BACKUP_FILE}" \
            --endpoint-url "https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com" \
            --region auto
          echo "Yüklendi: s3://${R2_BUCKET}/db-backups/${BACKUP_FILE}"

      - name: 30 günden eski yedekleri sil
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_BACKUP_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_BACKUP_SECRET_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ secrets.R2_BACKUP_BUCKET }}
        run: |
          CUTOFF=$(date -u -d '30 days ago' +%Y-%m-%dT%H:%M:%S 2>/dev/null \
            || date -u -v-30d +%Y-%m-%dT%H:%M:%S)
          ENDPOINT="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"

          aws s3api list-objects-v2 \
            --bucket "$R2_BUCKET" \
            --prefix "db-backups/" \
            --endpoint-url "$ENDPOINT" \
            --query "Contents[?LastModified<='${CUTOFF}'].Key" \
            --output text \
          | tr '\t' '\n' \
          | grep -v '^$' \
          | while read -r key; do
              echo "Siliniyor: $key"
              aws s3api delete-object \
                --bucket "$R2_BUCKET" \
                --key "$key" \
                --endpoint-url "$ENDPOINT"
            done
          echo "Temizlik tamamlandı."

      - name: Başarı özeti
        run: echo "Backup tamamlandi: $BACKUP_FILE - sonraki calisma: yarin 02:00 UTC"
