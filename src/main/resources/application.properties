spring.application.name=marine-management-system

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/marine_management
spring.datasource.username=marine_user
spring.datasource.password=maritime2025!

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional: SQL logging
logging.level.org.hibernate.SQL=DEBUG

# Basic Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=USER

jwt.secret=marineManagementSuperSecretKey2025ForCaptainTahir
jwt.expiration=86400000  # 24 hours in milliseconds